# Mission Control currently runs as a static single-file app and requires no runtime secrets.
# Add future configuration keys here with safe placeholder values only.
# Never commit real API keys, tokens, passwords, JWTs, private keys, or provider credentials.
